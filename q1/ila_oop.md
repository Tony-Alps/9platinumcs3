## Sari-Sari Store Inventory System

### 1. Encapsulation
I can use encapsulation in a Sari-Sari store inventory system by using it to store the data and info of each item in stock.

### 2. Abstraction
I can use abstraction in a Sari-Sari inventory system by only letting the code focus on the useful parts, not the useless ones in relation to a sari-sari store setting like your birthday, or what color the shirt you're wearing right now is. I would use it so that the code would become simpler, less cluttered with useless info, and so that it would run more efficiently.

### 3. Inheritance
I can use inheritance in a Sari-Sari store inventory system by using the structure of the code i could have made for the problem in ctskills. It would be inheritance because the code just inherited the structure of the code i made a while back so i don't start over completely from scratch.

### 4. Polymorphism
I can use polymorphism in a Sari-Sari store inventory system  by using the same function, sell_item(), to receive all types of payment methods and correctly deduct the number of items in stock, whether it is a liquid or a solid.

## Reflection
I think that the most useful in improving the sari-sari system would be abstraction. Abstraction, even though would not simplify the clutter of functions and other things, would be my best pick since the code and the variables i would be using would be at least going in the direction I wanted to go. I don't want to add variables in the code about tshirt color when the code should be monitoring a sari-sari store's inventory.